plugins {
    id 'java'
}

group = 'com.crazi'
version = '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    testImplementation platform('org.junit:junit-bom:5.9.1')
    testImplementation 'org.junit.jupiter:junit-jupiter'
    implementation(files('libs/DiceRoller-0.1.0.jar'))
}

test {
    useJUnitPlatform()
}